const appConfig = {
    apiPrefix: '/api',
    authenticatedEntryPath: '/home',
    unAuthenticatedEntryPath: '/sign-up',
    tourPath: '/',
    locale: 'en',
    enableMock: true,
}

export default appConfig
